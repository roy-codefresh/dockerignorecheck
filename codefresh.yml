version: '1.0'
steps:
  build:
    title: Building Docker Image
    type: build
    image_name: roycodefresh/test
    working_directory: ./
    dockerfile: Dockerfile
  echo:
    image: alpine
    commands:
      - echo $ID
    environment:
      - ID=${{build.imageId}}
