version: '1.0'
steps:
  mycomp:
    type: composition
    title: my composition
    composition:
      version: '2'
      services:
        db:
          image: mongo
    composition_candidates:
      test_service:
        image: alpine
        command: echo TOKEN = $KEY
    environments:
      - KEY = ${{TOKEN}}
