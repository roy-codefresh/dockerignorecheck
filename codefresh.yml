version: '1.0'
steps:
  p1:
    type: composition
    title: environment variables first form
    composition:
      version: '2'
      services:
    composition_candidates:
      test_service:
        image: alpine
        command: sh -c "echo TOKEN = $$KEY"
        environment:
          KEY: ${{TOKEN}}
  p2:
    type: composition
    title: environment variables second form
    composition:
      version: '2'
      services:
    composition_candidates:
      test_service:
        image: alpine
        command: sh -c "echo TOKEN = $$KEY"
        environment:
          - KEY=${{TOKEN}}
